/* =============================================================
    DOM [ Deal with elements ]

        - before  ( element || string )
        - after   ( element || string )
        - append  ( element || string )
        - prepend ( element || string )
        - remove
================================================================ */

// قبل العنصر كاامل string اولا element كيحط ال before ال
// بعد العنصر كاامل string اولا element كيحط ال before ال

// داخل العنصر فلتحت ديالو يعني كيولي اخر حاجة string اولا element فلولة كتحط ال append & prepend اما ال
// اما التانية ف كتحطهم فلول ديال العنصر يعني كيولي اول حاجة فيه



// remove()

// ويبان ليه none => block ويحول inspect ولكن يمكن لبنادم يدخل لل display: none لبغيتي تخفي عنصر كدير ليه css
// dom tree فال جافاسكريبت كتحيد العنصر نهائيا من ال remove اما ال